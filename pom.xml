<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>cn.seifly.jharness</groupId>
    <artifactId>jharness</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <name>JHarness</name>
    <description>Spring Boot 3 + PF4J 插件化框架</description>

    <properties>
        <java.version>17</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>

        <spring-boot.version>3.5.11</spring-boot.version>
        <pf4j.version>3.15.0</pf4j.version>
        <commons-lang3.version>3.14.0</commons-lang3.version>
        <druid.version>1.2.28</druid.version>
        <yauaa.version>8.1.0</yauaa.version>
        <kaptcha.version>2.3.3</kaptcha.version>
        <pagehelper.boot.version>1.4.7</pagehelper.boot.version>
        <fastjson.version>2.0.61</fastjson.version>
        <oshi.version>6.10.0</oshi.version>
        <commons.io.version>2.21.0</commons.io.version>
        <poi.version>4.1.2</poi.version>
        <velocity.version>2.3</velocity.version>
        <jwt.version>0.9.1</jwt.version>
        <jaxb-api.version>2.3.1</jaxb-api.version>
        <springdoc.version>2.8.16</springdoc.version>
        <mysql.version>8.2.0</mysql.version>
        <jakarta.version>6.0.0</jakarta.version>
        <knife4j.version>4.4.0</knife4j.version>
        <mybatis-plus.version>3.5.5</mybatis-plus.version>
        <!-- SDK 坐标（主应用与插件共用，与 plugin-framework 模块实际坐标一致） -->
        <sdk.groupId>cn.seifly.jharness</sdk.groupId>
        <sdk.artifactId>plugin-framework</sdk.artifactId>
        <sdk.version>1.0.0</sdk.version>

    </properties>

    <modules>
        <module>plugin-framework</module>
        <module>plugins</module>
        <module>plugin-app</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.pf4j</groupId>
                <artifactId>pf4j</artifactId>
                <version>${pf4j.version}</version>
            </dependency>
            <!--
              Lettuce 版本强制覆盖：spring-boot-dependencies 管理的 lettuce 版本低于
              Redis 8 Vector Set typed API（RedisVectorSetCommands / VSimArgs，自 6.7.0.RELEASE 起）所需，
              故在 SDK 依赖的 plugin-framework 之上，于根 pom 统一覆盖，保证编译期与运行期一致。
            -->
            <dependency>
                <groupId>io.lettuce</groupId>
                <artifactId>lettuce-core</artifactId>
                <version>6.7.1.RELEASE</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${commons-lang3.version}</version>
            </dependency>
            <dependency>
                <groupId>${sdk.groupId}</groupId>
                <artifactId>${sdk.artifactId}</artifactId>
                <version>${sdk.version}</version>
            </dependency>
            <dependency>
                <groupId>com.github.xiaoymin</groupId>
                <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
                <version>${knife4j.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <parameters>true</parameters>
                        <release>${java.version}</release>
                    </configuration>
                </plugin>
                <!-- Spring Boot 应用打包插件版本（plugin-app 使用） -->
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
